.container
  %h2.all_boards_tag
    = @board.title
  - @tasks.each do |task|
    = render 'commons/board', board: task
  = link_to new_task_path do
    .add_new_board
      .add_new_board_content
        .add_new_board_title
          %h2 + Add new card